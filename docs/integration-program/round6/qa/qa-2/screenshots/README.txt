No screenshots: every QA-2 finding this round is API-level (request in, database row or
JSON out) and has no rendered surface to capture. Rendering belongs to QA-1.

The local client bundle at this SHA could not be built either (see unmeasured.json U-03),
so a screenshot taken here would have been of the Aug 23 client, not of b5bed01.
