# Round 4 QA triage — three personas, one fix plan (2026-08-21, coordinator)

Sources: `qa/persona-1/` (visitor, 14 findings), `qa/persona-2/` (member, 22), `qa/persona-3/` (founder,
21). Live moved under the passes (`335058f` → `379d2e7` → `ace9d9d`): Rye's map/housing session is
landing again; every finding is labelled with its build. NOTHING DISPATCHED; fix lanes open next session
after re-measuring main and Rye's rulings on the two design questions below.

## Fix lanes (disjoint zones, ranked by harm)

**FIX-A · The tab-bar straddle class, one cure (HIGH).** P1-02, P2-01, P2-02, P2-03, P2-04, P3-F14: any
interactive row that lands on the fixed bar's line at first paint is visible and dead (calendar month
grid, Characters "Walk this path", Wallet Buy, module cards, map rows). One shared clearance in the
shell (scroll padding + bottom inset on content), then a fail-loud probe looping every reported page.
Zone: `MobileTabBar`/shell + per-page bottom spacing.

**FIX-B · The route-change spinner and layout shift (HIGH).** P1-01, P3-F21, P1-05: route-level Suspense
unmounts the whole shell (sometimes seconds, once a whole session; `React.lazy` has no retry), and data
sections land without reserved heights (CLS 0.35–0.50 on five routes). Keep the shell mounted, skeletons
with reserved heights, a lazy retry wrapper. Zone: `App.tsx` lazyPage + the five pages' loading states.

**FIX-C · Example and seed copy on real surfaces (HIGH for trust).** P1-03, P2-08, P3-F9: "(example
name)" and EXAMPLE NAME badges on the public calendar, the raw "Gratitude cycle lunar-000330 ends,
settlement is a human act after this" line, example resource rules named `ex-circle-land` with live
Edit/Remove and no chip. Copy that explains or hides; founder-facing example chips everywhere the
examples machinery renders. Zone: calendar header/copy, ResourcesAdminPanel, moduleCatalog copy.

**FIX-D · Founder-on-a-phone flow breaks (HIGH for founders).** P3-F1 (the setup walk has no phone
doorway), P3-F2 (the vision "apply" link lands on Form Submissions; needs a real drafts/publish surface
or a corrected route), P3-F3 (events-admin horizontal overflow +21–51px), P3-F7 (remembered-open drawer
swallows deep-link taps), P3-F5 (gathering times take device clock against the village-time promise),
P3-F6 (canRequest names stage when the blocker is forum lifecycle), P3-F4 (Go-live offers Members only
where the server will refuse; card already holds maxLifecycle). Zone: Admin.tsx nav state,
EventsAdminPanel, VillageMap walk doorway, VisionLayer link, GoLiveCard, canRequest message.

**FIX-E · Polish and a11y sweep (MED, one checklist lane).** Tap targets (footer 40px rows, drawer
36–24px, calendar controls 30–32px, lens chips 24–26px, Sign Out 87x20); text under 14px clusters (10px
moon-day labels, 12px chips/badges); contrast (profile tier labels 2.59:1, gratitude subhead 3.67:1,
brand teal 4.3:1 pairs); compass FAB overlaps (hero CTA, accordion chevrons, calendar corner); 404 page
brand + soft-404 200 + Go Home anchor; bell empty state with nothing to tap; phone accordion never
writes ?focus=; .ics feed last-4 carries no signal; agent panel six screens deep with no anchor;
/introductions fires its board fetch while off. Zone: shared components + the named pages, no server
logic.

**FIX-F · Maintenance queue (from the round, pre-QA).** Intake scanner first-party scoping (FIRST: 71
waivers deep); notify.ts introduction email case; docs/modules/introductions.md + shelf line; L6
matching_consent reads L7 consent_at; Characters sign-in link; Profile + register/magic-link ?next=;
stale "Modules On/Off" copy (ProjectHistory, EventsAdminPanel); stays.depart_on; NotifyPrefsPanel brief
line; Events.tsx inline waitlist/slots placements; "Ask your founders" CTA; hub item 17 lunar table.

**Routed elsewhere:** map iframe +400px interior, lens band 0x0 at phone widths, HELP badge (the map
session, standing B8); P2-06 cold first load (perf, measure before touching).

## Two design questions for Rye
1. **P2-05:** a shared link to a module that is OFF renders the generic 404 (deliberate invisibility).
   Keep (default: yes, off means invisible), or give off-modules a softer "nothing here yet" page?
2. **P3-F2:** where should org-draft publishing LIVE in Admin (its own tab, or inside Org Chart)? The
   vision link needs a real destination.

## What all three personas called world-class (keep it so)
The Living Map; the calendar's months-and-moons shape with the wheel, village time named, and the
working public .ics; the module library telling a visitor what the place can become; the honest admin
nav and Go-live judgment; example lifecycles that retire themselves; refusal copy that teaches; empty
states with a voice; zero horizontal overflow app-wide (one admin tab excepted); back never breaking.
