Review complete. Branch restored to the exact pre-rebase state (4e4e395 = origin/wt/map-org), working tree clean, probes left in gitignored `.qa-out/`.

---

# ADVERSARIAL REVIEW — PR #33 "The org lens" (wt/map-org)

## VERDICT: REJECT (blocking, one-line merge-resolution security regression) — SERVER half is clean, ARTIFACT half needs one fix.

Rebased onto origin/main **58e8f03** (rebased tip 86dcfe5, since restored to 4e4e395). `pnpm install --frozen-lockfile` clean. Rebase hit 4 conflicts: three were additive unions (trunk's 0083 `representsCircle`/`howChosen` vs the PR's `archetypes`), the fourth is the finding below.

---

## SERVER HALF — ACCEPT, ready to land

- **Cold `npx tsc -p tsconfig.tests.json --noEmit`: EXIT 0.** `pnpm check`: EXIT 0.
- **Org tests: 83 passed / 0 failed / 0 skipped** — villageExport 42, orgLoad 16, seatLapse 14, orgArchetypes 3 (ran against the S5 DB harness on 127.0.0.1:3307), orgJournal 8.
- The PR's whole server change is minimal and correct: it adds the pre-existing `org_roles.archetypes` column (migration `drizzle/0069_characters.sql`, `json NULL`) to the read path — `ROLE_COLS` + `rowToRole` in `server/lib/orgChart.ts`, and `archetypes: r.archetypes` in `server/index.ts:8318` on the `/api/org` seat payload (correctly OUTSIDE the `viewPeople` gate — it is non-identifying).
- **Input validation:** `archetypes` read via `asList()` (safe coercion of NULL/array/text → `string[]`). Values are only ever written by `server/lib/economySeed.ts` (the suggestion flow, `WHERE archetypes IS NULL`, constrained to known archetype keys) — never via the admin `/api/org` PUT (`WRITABLE` omits it), so nothing free-form reaches it. The interface comment ("the column the API accepted") slightly overstates the write path but the behavior is correct; not a bug.
- **seatLapse strand/double-count:** this PR does NOT modify the lapse logic (it's pre-existing trunk code). `isLapsed` keeps lapsed holders counted (no stranding — nothing is revoked); `structuralLoad` flags cross-key duplicates (`possibleDuplicates`) rather than silently merging or double-counting. Sound, and covered by the 14 passing seatLapse tests.

---

## ARTIFACT HALF — REJECT until one merge line is fixed

### BLOCKER (proven live): the badge-seal `data-bk` merge reopens the #29 stored XSS
`docs/prototypes/grounds-v0.html`, `refreshBadges()` ~line 2551.

The PR was authored on `def4b18` (pre-#29). Trunk #29 (`ace9d9d`, "Close the stored XSS in the living map's door surfaces") wrapped all four badge-seal `data-bk="${s.key}"` interpolations in `escq(s.key)`. The PR edits the first of those lines to add `data-brel`, and its version carries **raw** `data-bk="${s.key}"`. That single line is a real 3-way conflict; **resolving it to the PR's side drops the escaping and reopens the XSS.** (The other three `data-bk` lines auto-merge back to `escq` because only trunk touched them.)

I proved it end-to-end with playwright (chromium-1223, playwright resolved from `regen-civics-clean/node_modules`), driving the PR's own artifact vs the escq-resolved one and pushing a structure whose key is `zz"><img src=q onerror=...>` through the badge renderer:
- **PR-authored artifact (raw `data-bk`): `__PWNED = 4`** — the injected `<img>` executed; the key broke out of the attribute into a live element.
- **escq-resolved artifact: `__PWNED = 0`** — payload fully entity-escaped inside `data-bk`.

Structure keys are unsanitized in `restoreScene` (`s.key = r.key`), and `restoreScene` is reached by the config push and by draft-save/publish, so the sink is reachable by exactly the surfaces the brief named. Probe: `C:\Users\taren\Desktop\Amora\wt-map-org\docs\prototypes\.qa-out\xss_probe.cjs` (and `pr-raw.html`).

**Required to land:** the merge/rebase resolution of that line MUST be
`data-bk="${escq(s.key)}" data-bkind="${m.kind}" data-brel="${m.rel===false?'0':'1'}"`.
(`data-brel` is a computed `'0'`/`'1'` — safe.)

### What is sound in the lens (verified, not a concern)
- **The lens postMessage path carries no injectable label.** `client/src/pages/LivingMap.tsx` `pushLens` sends `{party, roles:[{name,state,archetypes}]}`; **no holder names are sent** (identity deliberately excluded). In the artifact, `roleApplyLive` uses role `name` only as a lowercased lookup key on an `Object.create(null)` map (no prototype pollution — verified), writes `_state` through the `roleStateIn` SeatState whitelist and `_tags` through `roleTagsIn` (strings <40 chars, cap 12, used only for the party filter). The visible seat label stays `x.s` from the scene and renders through `escq` in `seatRowH`. So the org lens adds **no new name/label DOM sink** beyond the `data-bk` line above.
- **Gates (all run from `docs/prototypes`):**
  - `verify_org_lens.js` (MODEL): ~50 checks, all PASS, exit 0 — includes prototype-key/injection tests (R2e–R2i, L9) and the SeatState whitelist round-trip.
  - `verify_org_ground.js` (SCREEN): ~28 checks, all PASS, exit 0 (composited keyline worst 70%).
  - **Revert-and-gate (negative controls all fire):** `BREAK=floor`→G2 red, `BREAK=plane`→G0e red, `BREAK=ink`→G5c red. The visual gate genuinely guards position, plane-order and legibility.
- **z-index:** new `#lens` plane `z:13` is unique and sits cleanly between seals(12) and vignette(20) (G0e); `#roleKey` `z:30` joins the existing chrome band, placement verified (L7c). No unallocated/colliding z-index.

### Secondary (non-blocking)
- **`verify_org_paint.js` ships intermittently red.** This PR ships it alongside `verify_org_ground.js`, but it's the superseded intermediate suite (the README's org section names only lens+ground as canonical). Across reps its P4 negative control at the crowded `council` building went FAIL (48%/41%) once, then PASS twice — the exact "artifact suites are intermittent" trap. Recommend deleting it as superseded, or relocating/loosening its P4 control at multi-satellite buildings, so the tree doesn't carry a flaky gate.
- **Patch re-runnability / byte-identity NOT exhaustively re-verified.** I validated the committed `grounds-v0.html` behaviorally (gates + live XSS probe), not by replaying all 30 `patch_h5_*.py` from the pre-patch blob or checking each patch's idempotency. The committed artifact is what ships and is gated; flagging this as not-fully-verified rather than pass.
- **"seat" → "Role" copy sweep:** the org-lens additions do NOT hardcode user-visible "seat" copy — the `#roleKey` legend already reads "open / part filled / full", and the "seat" hits in the diff are all comments/identifiers (invisible to `check-voice.mjs`). The coming rename won't snag on this PR. Nothing to change now.

---

## Bottom line
- **SERVER: ACCEPT / ready** — green cold-typecheck, `pnpm check`, and 83/83 org tests; the lone server change (archetypes read) is correct and column-backed.
- **ARTIFACT: REJECT / one-line fix** — the badge-seal `data-bk` merge must preserve `escq(s.key)` or it reintroduces the #29 stored XSS (proven executing). With that line resolved correctly, the org lens is sound (gates green, negative controls fire). Prune/repair the flaky `verify_org_paint.js` before relying on it, and note patch re-runnability was not exhaustively re-verified.

Branch left at 4e4e395 (unpushed, unchanged); probes in gitignored `docs/prototypes/.qa-out/`.