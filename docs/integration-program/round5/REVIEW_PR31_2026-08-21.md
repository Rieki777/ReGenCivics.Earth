All confirmed. Final picture is airtight:

- **Tenant claim HOLDS**: `#help{...bottom:var(--band-b-help,calc(70px+...))}` — reads `--band-b-help`, no literal `bottom`.
- **#29 (the lane's actual parent) introduced every escape** h8_3 claims is missing: `replies:t.replies||0`→`Number(t.replies)||0`, `${s.name}`→`${escq(s.name)}` in bannerHTML, plus cvrow/panel/attnCard/wallrow. The commit is literally titled "Close the stored XSS in the living map's door surfaces."
- **`.qa-out/` is gitignored** (line 133); my probes are invisible to git; working tree is clean; nothing committed/pushed.

I have decisive, driven evidence. Here is my verdict.

---

# ADVERSARIAL REVIEW — PR #31 "The top band, the vital drop and the help threads" (L8)

## VERDICT: REJECT

**Not ready to land.** The layout code is genuinely good, but the PR ships a verification apparatus that is **deterministically RED on its own correct artifact**, and its headline security claim describes a vulnerability its own parent commit already closed. Root cause in one sentence: **the lane re-derived every anchor and every security claim against `def4b18` (#19) — its docstring says so outright — but its actual parent is `ace9d9d` (#29, "Close the stored XSS in the living map's door surfaces"), which moved those lines and neutralised those sinks.**

## Provenance / instrument (all PASS)
- Base blob `ace9d9d:…/grounds-v0.html` = `b194509`. Applying `patch_h8_1..h8_4` in order reproduces the shipped artifact **byte-identical** (`85f5d2c`). Re-running each patch = all 15 edits skip, **+0 bytes** (idempotent).
- Rebase onto `origin/main` is clean and immaterial: L8 hunks at 583/649/904/6052-6184/6709; trunk's only `grounds-v0.html` delta (#34, `/introductions`) is at 5489 — zero overlap. I reviewed the working tree and left this shared worktree's history untouched.
- Every fix anchor re-grepped in the shipped file, each present exactly once; raw `+t.replies+' replies` = 0, hardcoded `#vdrop{…top:46px` = 0.

## The disqualifying defects (all driven, not read)

**1. The gate `qa/verify_help_l8.js` FAILS on the correct shipped artifact — deterministically, 3/365, every run.**
The three reds are all in §1 (the stored-XSS section):
- `FAIL the replies payload survived the import byte for byte [replies-byte-exact=false]`
- `FAIL the replies payload is on screen as literal text [replies present …=false]`
- `FAIL a poisoned place name reaches the document from restoreScene alone … [pre-tap nodes=0]`

The gate's §1 asserts (a) a string `t.replies` survives `restoreScene` byte-for-byte, and (b) `bannerHTML` injects one un-escaped place node. **Both are false on the lane's real base #29.** There is no green. The commit message stakes the lane on this gate ("one command covers the lane", "watched RED then green"); the author never actually ran it green on this artifact.

**2. The headline "LIVE STORED-XSS SINK" (replies, via restoreScene) does not reproduce.** `restoreScene` at line 5089 maps `replies:Number(t.replies)||0` (introduced by #29, verified in the `def4b18..ace9d9d` diff). `Number('<img…>')` → `NaN`, `NaN||0` → `0`. Driven: planting `replies:'<img src=x onerror=…>'` through `restoreScene` yields `SCENE.threads[0].replies === 0` (number); the help sheet renders **0 injected nodes, `window.__XSS` undefined**, with or without the escq change. The shipped comment even states the falsehood in code: *":5001 maps `replies:t.replies||0`, and `||` only replaces a falsy value"* — the actual line is `Number(t.replies)||0`. CLAUDE.md: "A comment that lies is how the next lane repeats the defect." The lane rewrote this comment "to stop overclaiming" and replaced one false claim with another. (The lane's own `_probe_thread_sinks.js:54` crashes on the working tree — `TypeError: replies.indexOf is not a function` — proving the same point.)

**3. Revert-the-fix leaves the gate identical (the brief's stand-alone REJECT condition).** Staged `x1_replies_raw` (restores the raw `t.replies` "defect"): gate returns `CHECKS 365 FAILS 3` — **the same three fails, byte-for-byte**, across paired alternating reps. The escq(t.replies) "fix" guards nothing reachable and the gate is blind to its removal. Contrast `x3_title_raw` and `x7_place_raw` (genuinely reachable string fields) → 7 fails: the gate *does* catch real defects, just not this one.

**4. h8_3's "WHAT THIS PATCH DOES NOT CLOSE" disclosure is stale — every sink it reports is already closed by #29.** Verified in current code: Overview `.cvrow` (:3497) `${escq(t.title)}…${escq(t.author)}…${escq(t.replies)}…${escq(t.last)}`; `bannerHTML` (:3163) `${escq(s.name)}`; vdrop Claim button (:5957) `claimQuest('${escj(q2.q)}',…)` (escj, the correct JS-string escape — not the escq breakout it claims). My reachable-sink census planted a hostile thread through `restoreScene` and drove `openPanel`/`openLoom`/`openDoor`/`#pbAttn`: **0 injected nodes on every surface.** The count still reachable through these surfaces is **0**, not the "4 per surface" the lane reports. The next lane would chase five already-closed sinks.

**5. The acceptance harness `_mut_run_l8.sh` yields false greens.** It asserts only `fails>0` per mutant and never requires the unmutated artifact to pass. Because baseline already fails 3, `x1_replies_raw`/`x2_replies_dropped` report as "caught" (3>0) while actually undetected — the exact silent-zero trap the gate's own header warns about, consuming the lane's own acceptance test.

## What IS sound (the layout work is real and well-guarded)
- **h8_1 vdrop-as-top-band-tenant**: revert `v1_vdrop_literal` → 3→14 fails (element at 46 while band publishes 41). Genuine.
- **h8_3 landscape cap/`max`**: revert `l1_cap_unread` → 3→31 fails, reproducing the exact named regression — the z-58 walk card covering the help sheet's title and close button on 844x390 / 667x375 / 740x360 / 568x320. Genuine fix, properly guarded.
- **h8_4 band floor**: revert `b1_floor_zero` → 3→13 fails (vdrop pinned at 6px on the vitals bar). Genuine.
- **h8_2 help sheet**: replaces a button that opened `#attnCard` (hidden `display:none!important` on `body.pocket`) with a working sheet; title/author/last/excerpt/id all correctly escaped (escq), `id` round-trips byte-exact for the deep link.

## Claim-by-claim
- **"help list is a tenant of the bottom band, reads `--band-b-*`, never writes its own `bottom:` literal"** — TRUE. `bottom:var(--band-b-help,…)`, registered tenant, no literal.
- **"--band-t-vdrop moves UNSET → …, NEVER the 46px fallback, assert on the var"** — mechanism TRUE and guarded, but the brief's pixel values are wrong: driven distinct set is **{41px (pockets), 42px (1280×800), 52px (1920×1080)}**, not "41/75/76/86". The falsifiable properties (measured≠literal, ≠46, element.top===var) hold.
- **"tipxvdrop 5887 → 0 on every portrait pocket"** — UNVERIFIED. The shipped gate measures vdrop×**walkCard** overlap, never tip×vdrop; the "5887→0" figure lives only in h8_1 prose and neither the gate nor I reproduced it.
- **"close a LIVE stored-XSS sink … reproduce BOTH first"** — the XSS half FAILS the "reproduce first" step (coerced to 0 by #29); the landscape-regression half reproduces cleanly and is fixed.

## Must change to land
1. Re-derive the gate's §1 against the real base #29: `replies` is numeric after restore — drop or invert the two `replies` string checks; the `bannerHTML` place-node check must expect **0** (escaped), not 1. Get the gate to a real green (0 fails) on the shipped artifact.
2. Correct the false claims: the commit message and the shipped `renderHelp` comment must not assert `replies:t.replies||0` / a live replies sink. State that #29 already coerces replies; keep escq(t.replies) as harmless hygiene if desired, but do not sell it as a vuln fix.
3. Rewrite or delete the "WHAT THIS PATCH DOES NOT CLOSE" disclosure — cvrow/banner/loom/door/vdrop-claim are all closed on #29; the reported counts and line numbers are def4b18 geometry.
4. Fix `_mut_run_l8.sh` to require the unmutated artifact to pass (0 fails) before trusting any mutant's `fails>0`.
5. Correct the vital-drop figures in the docstrings (41/42/52, not 41/75/76/86; the tip×vdrop claim needs a measurement or removal).

**Plainly: the map-facing code (help sheet + the three band fixes) is landable and its layout fixes are proven; the security narrative and the entire verification apparatus are not. As submitted — a PR whose own gate fails on its own code and whose lead claim is a vulnerability its parent already fixed — this is a REJECT.**

(No commits, pushes, or tracked-file changes made. Probes live in the gitignored `docs/prototypes/.qa-out/`; scratch/mutant staging under the session scratchpad.)