/**
 * Quest qualifier mappings.
 * Maps quest IDs to land projects and orgs that endorse or require each quest.
 *
 * Populated when stewards endorse quests through the platform.
 * Do not add hardcoded entries here, these are managed dynamically.
 */
export const QUEST_QUALIFIERS: Record<string, string[]> = {};
