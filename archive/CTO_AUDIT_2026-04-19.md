# CTO Audit — Remaining Work and Doc Cleanup

**Date:** 2026-04-19
**Auditor:** Claude (Cowork)
**Scope:** Every `FIXES_TO_MAKE_*` and `CLAUDE_CODE_PROMPT_*` doc in the repo root, plus `REMAINING_WORK_*`. Verified against actual source, not doc self-claims.

---

## Headline

Nine docs archived this pass (bringing total archive count to 30). Twenty docs remain in root, most with genuine outstanding work. One significant false-positive "done" claim surfaced: the V6 palette drift sweep (641 off-token hex literals still in place).

Gate 1 (truncation audit) passes. Gate 2 (palette drift) fails with 641 off-token hex literals. Gate 3 (typecheck) cannot run in this sandbox and must be run locally by Rye.

### Re-verification corrections (2026-04-19 afternoon pass)

After Rye flagged that some items marked "not done" or "Rye-only" had actually shipped, the following status changes apply to the table below:

- **INK_REVEAL doc: DONE and archived.** The `useAnimationLayer.ts` hook only watches `img.blur-up`. Pages without a hero `<img>` (Home, Fund, Game, Tokenomics, Land) use `ParallaxSection` / CSS backgrounds / particles and cannot meaningfully receive `.blur-up`. Bionomics and Team do have hero `<img>` tags and both have `.blur-up`. `.ink-reveal` is on the h1 of Home, Fund, Game, Tokenomics, Land, Team (6 pages); Bionomics uses `.word-reveal` on its single-word hero (functionally equivalent). Nothing meaningful left to wire.
- **Riverside room URL: DONE.** `client/src/pages/Schedule.tsx` has `RIVERSIDE_INFO.roomUrl = "https://riverside.com/studio/rieki-cordon-riekis-studio?t=243a36b4d9fdbc785c4b"`. Not a Rye-pending item.
- **GEMINI_API_KEY: present in `.env`.** Not expired. Parts 14/15 seasonal/governance images can be regenerated at any time.
- **Cultivator rename: CLEAN.** Three references total (`gameRoles.ts:711` "The Cultivator of Relations", `TierBadge.tsx:12` tier badge, `regionalCostData.ts:177` cost tier). No drift; all consistent.
- **Fix 2 in `FIXES_TO_MAKE_2026-04-11.md` (seasonal quest headings): DONE.** `Quest.tsx:625` uses `text-white` + `textShadow: "0 2px 8px rgba(0,0,0,0.5), 0 1px 3px rgba(0,0,0,0.4)"` on the seasonal h3, and `Quest.tsx:634` does the same on the tagline. The spec's line numbers (1050-1080) were stale; the current render is at 615-645 inside `SeasonCarousels`.
- **Fix 4 in `FIXES_TO_MAKE_2026-04-10.md` (crowd pool readability): REOPENED with new direction.** Two crowdpool views have diverged: `CrowdPoolingProjects.tsx:655` uses the intended transparent-glass treatment (`bg-white/5 backdrop-blur-sm border-[#7dd87d]/20` on the dark green gradient, white text), while `CrowdPoolingCampaigns.tsx:173` still uses solid `bg-white` cards with dark green text. Rye confirmed on 2026-04-19 that the transparent-glass style is the preferred design and should NOT be reverted to solid white. Direction: migrate `CrowdPoolingCampaigns.tsx` to match `CrowdPoolingProjects.tsx` (transparent cards on the dark radial gradient page background, white text with text-shadow, `backdrop-blur-sm` for glass effect). Priority: MEDIUM.

---

## Archived this pass

Moved to `archive/` because the work is complete or the doc is stale:

| Doc | Reason |
|---|---|
| `REMAINING_WORK_2026-04-07.md` | Superseded by `REMAINING_WORK_2026-04-08.md` |
| `CLAUDE_CODE_PROMPT_2026-03-28_PART5.md` | Recording flow shipped. `ReplayButton` wired at `client/src/pages/Schedule.tsx:37-53`, `RecordingsSection` at line 60+, `RIVERSIDE_INFO` live, zero `ZOOM_INFO` references remain, tRPC `recordings.byEventId` and `recordings.list` both wired. |
| `CLAUDE_CODE_PROMPT_2026-03-28_QUEST_LOCK.md` | All components exist: `client/src/hooks/useQuestUnlocks.ts`, `LockedQuestCard.tsx`, `HeroQuestCard.tsx`, `SeasonProgressRing.tsx`. Wired in `Quest.tsx:41,149`. Hero images present. |
| `CLAUDE_CODE_PROMPT_2026-04-07_CITIZENSHIP_BATCH.md` | `checkCitizenshipTiers` in `server/routes/batchJobs.ts`, cron registered in `server/_core/index.ts:615-643` at `/api/cron/nightly-batch`, verification script `scripts/verify-citizenship-batch.ts` present. |
| `CLAUDE_CODE_PROMPT_2026-04-07_POST_CTO.md` | All code-side launch blockers closed. Only remaining items are Rye-only (C2 GCP Maps restriction, H8 Sentry DSN, R2-21 seed run). |
| `CLAUDE_CODE_PROMPT_2026-04-08_COMMUNITY_AGREEMENTS.md` | Parts 1-15 shipped per `COMMUNITY_AGREEMENTS_IMPLEMENTATION_LOG.md`. `/community/guidelines` live, agreements router, seeded 6 agreements, propose/vote UI all verified. |
| `CLAUDE_CODE_PROMPT_2026-04-09_LOOMIO_INTEGRATION.md` | Deliverable was a research + spec doc, output exists at `LOOMIO_INTEGRATION_SPEC_2026-04-09.md`. Superseded by `CLAUDE_CODE_PROMPT_2026-04-09_LOOMIO_OVERHAUL.md` for implementation. |
| `CLAUDE_CODE_PROMPT_2026-04-17_V5_RETRY.md` | All 5 fixes verified: drift sweep script exit-zero on its own scope, `WhoHoldsVoteChart` imports `governanceSlices`, `/map` h1 present, husky pre-commit and lint-staged wired. |
| `CLAUDE_CODE_PROMPT_2026-04-07_INK_REVEAL.md` | Re-verified 2026-04-19: 6 pages have `.ink-reveal` on h1 (Home, Fund, Game, Tokenomics, Land, Team); Bionomics uses `.word-reveal` (equivalent); both pages with hero `<img>` (Team, Bionomics) have `.blur-up`. The other five pages cannot use `.blur-up` because they render hero via `ParallaxSection`/CSS backgrounds/particles — the hook only watches `img.blur-up`. No outstanding work. |

---

## Remaining docs, status, and what each actually needs

### Reference (keep in root, not work queues)

| Doc | Role |
|---|---|
| `CLAUDE_CODE_PROMPT_2026-03-31_GAME_SYSTEM.md` | Full 5-phase game system spec. Used as reference by Claude Code when touching game features. |
| `CLAUDE_CODE_PROMPT_2026-04-03_CHARACTER_ART.md` | Visual style guide plus image-gen prompts for the 13 role illustrations. Reference every time character art or role illustrations are touched. |
| `REMAINING_WORK_2026-04-08.md` | Status index. Will need a next-pass update after the items below are addressed. |

### Active with real outstanding work

Priority key: `LAUNCH BLOCKER` = before Earth Day 2026 (Apr 22). `HIGH` = this sprint. `MEDIUM` = next sprint. `POST-LAUNCH` = after Earth Day.

#### CLAUDE_CODE_PROMPT_2026-04-01_FIXES_AND_TIERS.md — MOSTLY DONE
- Remaining: confirm the "Cultivator" role rename is applied everywhere, not just one spot. Grep currently finds inconsistent coverage. Priority: MEDIUM.
- Remaining: confirm `home-desktop.webp` and `home-mobile.webp` were regenerated (referenced but not visually verified). Priority: MEDIUM.

#### CLAUDE_CODE_PROMPT_2026-04-01_UNIFIED_BUILD.md — REFERENCE (7-track master plan)
- Not a discrete work order. Individual tracks map to other active docs. Keep for cross-spec reconciliation. Priority: REFERENCE.

#### CLAUDE_CODE_PROMPT_2026-04-07_INK_REVEAL.md — DONE (archived 2026-04-19)
- Re-verified: `.ink-reveal` on h1 for Home, Fund, Game, Tokenomics, Land, Team. Bionomics uses `.word-reveal` (functionally equivalent staggered reveal for a single-word hero).
- `.blur-up` on hero `<img>` for Team and Bionomics. The other five pages do not use `<img>` for their hero (they use `ParallaxSection`, CSS backgrounds, or particles), and `useAnimationLayer.ts` only watches `img.blur-up`. Nothing to wire.

#### CLAUDE_CODE_PROMPT_2026-04-07_OG_IMAGES.md — MOSTLY DONE
- OG images exist in `client/public/og/` (apply, bionomics, community, connect, crowd-pooling, economy, features, fund, game-mechanics, game, governance, hymn-book, land, local-food-economy, map, opportunity, plus .webp variants).
- Remaining: confirm each of the 7 primary route images is 1200x630 and wired to the correct route's OG meta tag. Tokenomics "How Returns Flow" duplicate section cleanup status unclear — needs eyes. Priority: MEDIUM.

#### CLAUDE_CODE_PROMPT_2026-04-07_POST_AUDIT_CLEANUP.md — MOSTLY DONE
- Track A (CSP nonce): `server/_core/nonce.ts`, `cspNonceMiddleware` in `server/_core/security.ts`, `nonce="{{NONCE}}"` placeholders in `client/index.html`, `'unsafe-inline'` removed per comments.
- Remaining: end-to-end verify nonce rotation on a real request and confirm no console CSP violations in production build. Priority: HIGH but RISKY per doc.
- Tracks B, C: presumed fixed, not line-by-line verified.

#### CLAUDE_CODE_PROMPT_2026-04-09_GOVERNANCE_PUSH2.md — CODE DONE, DEPLOY PENDING
- All Tracks A-H coded: `server/routes/governance.ts` (41 KB), `gratitude.ts`, `players.ts` with `setStoryteller`, `BridgeHypha.tsx` wired, `loomio.ts` webhook receiver, `governanceJobs.ts` using `viem` for Base RPC, migrations 0108-0111 on disk.
- Remaining for Rye: run migrations 0108-0111 on Railway via `npx tsx scripts/run-migration.ts --all` (or the runner's current command). Set any new env vars called out in the doc (`LOOMIO_API_KEY`, `LOOMIO_WEBHOOK_SECRET`, etc.). Priority: HIGH. **Human-only.**

#### CLAUDE_CODE_PROMPT_2026-04-09_LOOMIO_OVERHAUL.md — HUMAN DEPLOY ONLY
- Spec complete, brand SVGs and CSS ready in `./loomio Governance Tools/`.
- Remaining for Rye: THEME_* env vars on Loomio Railway service, Docker rebuild with brand SVGs baked in, CSS injection via `THEME_STYLESHEET_SRC` or admin panel, OIDC setup, org structure. Priority: HIGH. **Human-only.**

#### CLAUDE_CODE_PROMPT_2026-04-09_PARTS_14_15.md — MOSTLY DONE
- Part 14 (Roles Dialogue quest): migration 0107 applied, forum post seeded, `roles-dialogue.png` present, Community.tsx card wired.
- Part 15A-F (readability audit, seasonal cycle image, vote distribution, voice-holders diagram, nav highlights, Governance Evolution removal): all shipped.
- Remaining for Rye: refresh `GEMINI_API_KEY` (expired), re-run `scripts/generate-*-image.py` for the 4 seasonal/governance images currently on PIL fallbacks. Turn ON the Riverside Zapier automation. Confirm Riverside Pro hours cover Season 2. Priority: MEDIUM.

#### CLAUDE_CODE_PROMPT_2026-04-17_SPRINT3_WORLD_CLASS.md — ~70% done
- A1 (monogram fallback), A4 (44px hit-area at `index.css:1168`), A5 (`useLayoutEffect` in `SEO.tsx:43`) done.
- A2 (per-page `<main>` landmark): `App.tsx:378` wraps all routes in `<main>` but individual pages do not set `id="main-content"`. Needs audit if per-page structure is the spec.
- A3 (heading hierarchy): only spot-checked. At least Bionomics still needs h3→h2 promotion on first section.
- Part B (25 visual ideas): CSS/keyframes defined (`breathing-cta`, `breathing-gradient`) but not wired to JSX. Priority: HIGH.

#### CLAUDE_CODE_PROMPT_2026-04-18_FINISH.md — ~50% done
- Phase 1.1 `LandscapeSVG` wired on 4 pages (Home, Bionomics, Tokenomics, Community).
- Phase 1.2 `BentoGrid`: zero pages wired. Spec target: /apply, /map.
- Phase 1.3-1.6 (BloomMarker, Sparkline, ForYouLabel, Breadcrumbs): component files exist; only Breadcrumbs wired via PageWrapper.
- Phase 2 `RiverDivider`: wired on Governance only. Spec target: also Community, Bionomics.
- Phase 4 (ink-reveal, OG): see INK_REVEAL row above. OG images exist but route-by-route wiring not verified.
- Phase 5 (20 ideas): components exist, JSX placement minimal. Priority: HIGH.

#### CLAUDE_CODE_PROMPT_2026-04-18_POLISH_SPRINT4.md — ~60% done
- Week 0 F1 (music slug routes) done: `HymnPlayer.tsx:8` uses `useParams<{ slug: string }>()`.
- Week 0 F2 (radial menu) done: arc 160°, radius 128 at `WizardRadialMenu.tsx:85-91`.
- Week 0 F3 (More menu logo) done: `regencivics-logo-light-transparent-rounded.webp`.
- Weeks 1-5: systematic placement of signature visuals and skeletons pending. Priority: HIGH.

#### FIXES_TO_MAKE_2026-04-08_MOBILE_SAFARI.md — NOT STARTED (~30 items)
- Document is a work queue, not a completion log. No code changes found for any of A1-A5 (global mobile), B1-B11 (page layout), C1-C2 (Guide widget), D1-D5 (game simulator), E (legal), F (crowdpooling), G (schedule), H (team), I (profile), J (nav).
- Priority: HIGH. Multiple items are "Critical" for mobile ship quality.

#### FIXES_TO_MAKE_2026-04-09_GOV_OVERHAUL.md — CODE DONE, DEPLOY PENDING
- Fixes 1-7 all have code shipped per the doc's own evidence columns.
- Fix 8 (Loomio overhaul), Fix 9 (OIDC), Fix 10 (org structure): all "HUMAN STEP REQUIRED" and pending Rye's Railway/Loomio work.
- Priority: HIGH. **Mostly human-only.**

#### FIXES_TO_MAKE_2026-04-10.md — mostly done
- Fix 1 (Magicians song removed), Fix 2 (intro video gate), Fix 3 (map filter bottom-20) done and verified.
- Fix 4 (crowd pool readability): REOPENED with new direction per Rye 2026-04-19. `CrowdPoolingProjects.tsx:655` already uses the intended transparent-glass treatment (`bg-white/5 backdrop-blur-sm border-[#7dd87d]/20`, white text on dark gradient). `CrowdPoolingCampaigns.tsx:173` still on solid `bg-white`. Migrate `CrowdPoolingCampaigns.tsx` to match the glass pattern. Priority: MEDIUM.
- Fix 5 (quest village redesign): blocked on art from Rye. Priority: POST-LAUNCH.
- Fix 6 (menu top bar overlap): no CSS found. Needs Rye screenshot. Priority: HIGH but blocked.

#### FIXES_TO_MAKE_2026-04-11.md — 12 of 12 verified
- Fixes 1-12 all verified in code.
- Fix 2 re-verified 2026-04-19: `Quest.tsx:625` seasonal h3 uses `text-white` + `textShadow: "0 2px 8px rgba(0,0,0,0.5), 0 1px 3px rgba(0,0,0,0.4)"`; line 634 tagline uses `text-white/90` + matching shadow. Style is applied in the `SeasonCarousels` component which renders per season, so all four seasons inherit it.

#### FIXES_TO_MAKE_2026-04-17.md — mostly done
- Fixes 1-5 verified in code (safe-prose utility, ScrollToTop bottom-left, MobileTableOfContents pin, Bionomics hero layout, WhoHoldsVoteChart SVG).
- Fixes 6-13: claimed CODED but need live-page verification by Rye. Priority: MEDIUM.

#### FIXES_TO_MAKE_2026-04-18.md — ~50% done
- Part 0 (F1 music, F2 radial, F3 More logo) done.
- Part 1 A1-A5: A1, A4, A5 done. A2 and A3 partial.
- Parts 2 and 3 (49 signature visual ideas): zero wired to JSX. Priority: HIGH.

#### FIXES_TO_MAKE_2026-04-18_POST_AUDIT.md — ~90% done, blocked on Gate 3
- Verification table 15 items all wired: V3 LOI h1→h2, V5 confetti-fall keyframe, V6 tier-badge-glow, V14 category-accent-bar, N1-N4 nav accents.
- **Gate 3 (typecheck) cannot run in the sandbox (no `tsx`, can't `pnpm`).** Rye must run `pnpm typecheck` locally. If failures, Claude Code fixes before commit.
- Post-session deferred: V12 toast-garden, V13 quest-disclosure.

#### FIXES_TO_MAKE_VISUAL_AUDIT.md — PARTIAL, FALSE POSITIVE ON V6
- V1 (palette checker script), V3 (season constants via tokens) verified in code.
- **V6 "Blue/purple drift removal" claims DONE but is not.** A palette drift run on 2026-04-19 finds **641 off-token hex literals across `client/src/`.** Top offenders: `LivingTree.tsx` (36), `Game.tsx` (36), `GlobeMap.tsx` (35), `Community.tsx` (33), `SeasonalRhythmSection.tsx` (29), `PlayerProfile.tsx` (22), `Quest.tsx` (22). Top drift hexes: `#1a3a1f` ×15, `#f8fdf8` ×14, `#0f2d1a` ×11, `#3d6b4a` ×10, `#1a2e1a` ×10.
- V4 (Admin/governance components): claimed DONE, not independently verified. Given V6 is false, this claim is high-risk. Priority: HIGH for the re-sweep.

---

## Rye-only outstanding items (pulled forward from the docs above)

| Item | Priority | Notes |
|---|---|---|
| C2 GCP Maps API key restriction | LAUNCH BLOCKER | ~5 min in Google Cloud Console. Restrict to `regencivics.earth`. |
| H8 Sentry DSN + source maps verification | HIGH | Confirm `VITE_SENTRY_DSN` set on Railway and source maps are uploaded on build. |
| R2-21 heal-the-land seed run | HIGH | Script in repo, needs real credentials and Rye's user ID. |
| Riverside → YouTube Live connect | MEDIUM | Settings → Streaming in Riverside. |
| Zapier: turn ON the Riverside Zap | HIGH | "New YouTube videos to Riverside webhook POST." Published but toggled off at last check. |
| Run migrations 0108-0117 on Railway | HIGH | `npx tsx scripts/run-migration.ts --all`. Governance Push 2 + Privy + historical claims depend on these. |
| Loomio Railway: THEME_* vars + Docker rebuild | HIGH | Per `CLAUDE_CODE_PROMPT_2026-04-09_LOOMIO_OVERHAUL.md`. |
| Loomio OIDC setup + org structure | MEDIUM | Per same doc. |
| `pnpm typecheck` (Gate 3) | HIGH | Sandbox can't run it. Needs Rye's Windows machine. |
| Git lock file `.git/index.lock` | LOW | Stale lock from a previous session blocks `git mv` and `git add`. Rye should remove it manually at some point. |

**Cleared from this list on 2026-04-19 re-verification:** Riverside room URL (DONE, `Schedule.tsx` has the real URL), GEMINI_API_KEY refresh (present in `.env`, not expired), Cultivator rename (no drift found).

---

## Code work surfaced, not yet done, that Claude Code could pick up

These are the concrete action items Claude Code should take, sorted by priority:

1. **(HIGH) Re-run the V5 palette drift sweep across 641 hex literals.** Sweep `client/src/**/*.{ts,tsx}` to replace off-token hexes with tokens from `design-tokens.ts`. Top-offender files listed above. The V6 "DONE" claim in `FIXES_TO_MAKE_VISUAL_AUDIT.md` is false.
2. **(HIGH) Begin FIXES_TO_MAKE_2026-04-08_MOBILE_SAFARI.md Section A (global mobile) and B (page layout).** Thirty iPhone Safari issues with zero code work so far. At minimum land A1 (horizontal scroll), B10 (viewport), E3, G1 as "Critical."
3. **(HIGH) FIXES_TO_MAKE_2026-04-18.md Parts 2 and 3 JSX wiring.** Forty-nine signature visual ideas specified, zero wired. Start with the 3-5 with highest visual impact and ship in small batches with Gate 1-3 passes.
4. **(HIGH) CLAUDE_CODE_PROMPT_2026-04-18_FINISH.md Phase 1.2-1.6 and Phase 2 completion.** Wire BentoGrid, BloomMarker, Sparkline, ForYouLabel, and place RiverDivider on Community and Bionomics.
5. **(MEDIUM) Audit V4 (Admin/governance component migration).** Given V6 was a false positive, independently confirm the 10 Admin/governance files actually migrated to tokens.
6. **(MEDIUM) A3 heading hierarchy audit.** Systematically run through all pages and promote skip 1→3 patterns. At minimum Bionomics `h3` at lines 160, 306, 400 (section headers inside `CollapsibleCard`/`InteriorArticleCard`/`PrinciplesCard` components) should be `h2` variants since no `h2` sits above them within their sections.

---

## Verified-but-not-run items (would need live DB or browser)

I was not able to do these from the sandbox:

- Query `_migrations_applied` on Railway to confirm 0086-0117 are applied. The sandbox's pnpm symlink layout blocks `mysql2` loading via ad-hoc scripts, and `tsx` isn't installable without root. Use the project's own `npx tsx scripts/run-migration.ts --status` locally.
- Run `pnpm typecheck` (Gate 3). Same constraint.
- Run `npx tsx scripts/check-palette.ts` through the project's dev loader. Ran equivalent logic via plain `node` and got the 641-finding number above.
- Visual QA on the deployed site, e.g. confirm OG images render at 1200x630, CLS < 0.1 on each page, prefers-reduced-motion respected in practice.

---

## Gate status summary

| Gate | Status | Source |
|---|---|---|
| 1 — truncation audit | PASS | `python3 scripts/audit-truncation.py` scanned 665 files, 0 truncated, 0 suspicious |
| 2 — palette drift | **FAIL** | 641 off-token hex literals; top file `LivingTree.tsx` with 36 |
| 3 — typecheck | BLOCKED | Cannot run in sandbox; Rye runs `pnpm typecheck` on his machine |

Per the ship-gate rule in `CLAUDE.md` ("Three gates must pass before marking any fix, task, or feature VERIFIED, DONE, or shipped"), Gate 2 alone blocks any "VERIFIED" claim on FIXES_TO_MAKE_VISUAL_AUDIT V6, and any sprint that depends on the palette being clean.

---

## Claude Code handoff prompt (copy-paste this to Claude Code)

The following prompt is ready to paste into a fresh Claude Code session in the `regen-civics-clean` repo. It covers every code item surfaced above, in priority order, with concrete file paths, evidence requirements, and Ship Gate enforcement. Rye's only job is to paste it and then run the three gates + git push when the work lands.

---

### PROMPT START

You are continuing work on `regen-civics-clean`. A CTO-style audit was completed on 2026-04-19 (see `CTO_AUDIT_2026-04-19.md` in repo root for full context). This prompt is the code-side follow-through.

Read these first, in order, before writing any code:
1. `CLAUDE.md` (repo root) — especially the Writing Rules (no em-dashes, no contrast-framing, no AI words), the Ship Gate protocol, and the Evidence-column requirement for FIXES docs.
2. `CTO_AUDIT_2026-04-19.md` — the full audit. The "Code work surfaced" section is your work queue.
3. `.claude/skills/regen-ship-gate/SKILL.md` — the mandatory three-gate protocol before any "VERIFIED" or "DONE" claim.
4. `client/src/lib/design-tokens.ts` — the canonical palette. All hex replacements source from here.

**Operating rules for this session:**

- Do not mark any fix "VERIFIED" or "DONE" without running all three gates green: `python3 scripts/audit-truncation.py`, palette check, `pnpm typecheck`. If `pnpm typecheck` cannot run in your environment, stop before the `VERIFIED` claim and hand back to Rye to run it locally.
- Commit in small batches (5-10 files, one logical change per commit). Never batch unrelated fixes into one commit.
- For every row you touch in any `FIXES_TO_MAKE_*.md`, add or update the Evidence column with concrete `file:line` references, grep output, or script output. No evidence = status stays `CODED`, never `VERIFIED`.
- Do not use em-dashes anywhere. Use a comma, period, colon, or rewrite.
- Maximum autonomy. Try things before asking permission. Only surface to Rye when there is literally no way to proceed without a human action (Railway env var, Google Cloud Console, browser action). When a human step is unavoidable, finish everything else first and present a single minimal list at the end.
- **Honor current direction over stale spec.** Some FIXES docs were written weeks ago. If the current code has evolved past the spec in a way that is clearly better (different styling approach, new component pattern, replaced page structure), preserve the current direction and update the fix spec to match, do NOT revert to the old spec. Concrete example already confirmed: the crowdpool card treatment is transparent glass (`bg-white/5 backdrop-blur-sm`), not solid white; migrate any remaining solid-white crowdpool cards to the glass pattern, do not undo the glass pattern. When in doubt, match the newer page to the newer pattern, not the other way around.

**Work queue, in priority order. Do not skip ahead, but do parallelize within a priority when safe.**

#### P0 — Spec-vs-current drift scan (30 minute budget)

Before touching any fix, do a quick drift scan. For each "NOT STARTED" or "IN PROGRESS" fix you are about to pick up, read the relevant files and confirm the spec still reflects the current design intent. If the current code has already moved past the spec in a way that is clearly better (transparent glass cards vs solid white, new component vs old inline JSX, refactored page vs original), leave the improved version in place and rewrite the fix to align with the new direction. Record any drift findings as a bullet list at the end of your session report so Rye can confirm.

Known drift already caught on 2026-04-19 (do not revert these):
- CrowdPoolingProjects uses transparent-glass cards. The direction for CrowdPoolingCampaigns is to match Projects, not revert Projects to solid white.
- Bionomics h1 uses `.word-reveal`, not `.ink-reveal`. Both are valid; do not replace.
- Fix 2 in 2026-04-11 seasonal headings is DONE with white+textShadow at `Quest.tsx:625,634`; the spec's line numbers (1050-1080) are stale.

#### P1 — Palette drift sweep (V6 re-do)

**Goal:** Reduce the 641 off-token hex literals in `client/src/**/*.{ts,tsx}` to zero, or to a documented exception list.

**Method:**
1. Port the palette check to a no-dep node script. The existing `scripts/check-palette.ts` requires `tsx`. Create `scripts/check-palette.mjs` as an ESM equivalent (read `design-tokens.ts` via a text parse for the exported hex constants, scan `client/src/**/*.{ts,tsx}` for `#[0-9a-fA-F]{3,8}` literals not in the allowlist, print offenders grouped by file).
2. Run it. Confirm the 641-literal baseline.
3. Work through the top offenders in order: `LivingTree.tsx` (36), `Game.tsx` (36), `GlobeMap.tsx` (35), `Community.tsx` (33), `SeasonalRhythmSection.tsx` (29), `PlayerProfile.tsx` (22), `Quest.tsx` (22). For each file, replace each off-token hex with the nearest token from `design-tokens.ts`. When no token is close, add a new named token to `design-tokens.ts` rather than inlining the hex.
4. Commit per file or per 2-3 small files. Commit message: `chore(palette): migrate <filename> to design tokens (<count> replacements)`.
5. After each commit, run all three gates.
6. When the script reports zero, update `FIXES_TO_MAKE_VISUAL_AUDIT.md` V6 row with: Evidence = "`node scripts/check-palette.mjs` → 0 offenders (<date>)"; Status = `VERIFIED`.

**Exit criterion:** Palette check reports 0 offenders, or Rye has reviewed and approved a written exception list checked into the repo.

#### P2 — Mobile Safari fixes (FIXES_TO_MAKE_2026-04-08_MOBILE_SAFARI.md)

**Goal:** Land every item marked "Critical" and at least half of the "High" items.

**Method:**
1. Read the doc top-to-bottom. It's organized by section: A (global mobile), B (page layout), C (Guide widget), D (game simulator), E (legal), F (crowdpooling), G (schedule), H (team), I (profile), J (nav).
2. Start with A1 (horizontal scroll), B10 (viewport meta), E3 (legal page overflow), G1 (schedule layout on small width). These are spec'd as Critical and unblock the rest.
3. For each fix, write the code, run the gates, commit. Update the doc's Evidence column with `file:line` for each change.
4. Budget: ~2 hours of wall-clock in this session on mobile Safari. Move to P3 once the Critical items are in.

#### P2.5 — Migrate `CrowdPoolingCampaigns.tsx` to transparent-glass card style

**Goal:** Bring `CrowdPoolingCampaigns.tsx` cards into line with `CrowdPoolingProjects.tsx:655` so both crowdpool views share the same glass aesthetic.

**Method:**
1. Read `CrowdPoolingProjects.tsx:600-700` for the reference pattern: dark radial gradient page background, cards with `bg-white/5 backdrop-blur-sm border-[#7dd87d]/20 rounded-xl`, white text with `/70` and `/50` opacity scales for secondary text, `text-[#7dd87d]` for accents.
2. In `CrowdPoolingCampaigns.tsx`:
   - Change the page root `bg-gradient-to-b from-[#f8f5f0] to-[#f0ebe0]` to a dark gradient consistent with Projects (or confirm Rye wants light bg kept; if kept, then cards stay dark-on-light and don't need the glass treatment).
   - Change card div at line 173 from `bg-white rounded-xl overflow-hidden shadow-lg ... border border-[#7dd87d]/40` to `bg-white/5 backdrop-blur-sm rounded-xl overflow-hidden border border-[#7dd87d]/20 hover:border-[#7dd87d]/50 transition-all`.
   - Change text colors from `text-[#1a472a]`, `text-[#1a472a]/85`, `text-[#1a472a]/90` etc. to `text-white`, `text-white/80`, `text-white/70`, `text-white/50`.
   - Add `textShadow: "0 1px 2px rgba(0,0,0,0.3)"` to h3 and body paragraphs where the card sits over the hero image.
   - Keep accent color uses as-is (`text-[#7dd87d]`, `bg-[#7dd87d]`).
3. Verify in `pnpm dev` on `/crowd-pooling-campaigns` that the cards read well against the page background. Rye should review a screenshot before VERIFIED.
4. Update `FIXES_TO_MAKE_2026-04-10.md` Fix 4 row with the new Evidence: `CrowdPoolingCampaigns.tsx:173` now matches `CrowdPoolingProjects.tsx:655` pattern.

**Exit criterion:** Both crowdpool views render with the same card style. Gates 1-3 green. Rye visually confirms.

#### P3 — FIXES_TO_MAKE_2026-04-18.md Parts 2 and 3 (signature visuals wiring)

**Goal:** Wire at least 10 of the 49 signature visual ideas into JSX, focused on pages with the highest visitor impact.

**Method:**
1. Read Parts 2 and 3 of `FIXES_TO_MAKE_2026-04-18.md`. Each idea has a spec and a suggested placement.
2. The target components already exist: `BentoGrid`, `BloomMarker`, `Sparkline`, `ForYouLabel`, `RiverDivider`, and the Phase 5 set. See `CLAUDE_CODE_PROMPT_2026-04-18_FINISH.md` for component contracts.
3. Prioritize: Home, Fund, Game, Community, Apply (where they exist). One commit per page, describing which visuals went where.
4. Update each wired row in `FIXES_TO_MAKE_2026-04-18.md` with Evidence.

#### P4 — CLAUDE_CODE_PROMPT_2026-04-18_FINISH.md Phases 1.2-1.6 and 2

**Goal:** Wire `BentoGrid` (Phase 1.2), `BloomMarker` (1.3), `Sparkline` (1.4), `ForYouLabel` (1.5) on at least one page each; place `RiverDivider` (Phase 2) on Community and Bionomics.

**Method:**
1. Read the phase-by-phase spec in that doc. Each phase names the target pages.
2. Implement phase by phase, one commit per phase. Evidence: `file:line` of each `<BentoGrid />`, `<BloomMarker />`, etc. in JSX, plus a screenshot path if Rye has provided one.

#### P5 — V4 (Admin/governance) token migration audit

**Goal:** Independently verify whether the 10 Admin/governance files actually migrated to design tokens, given V6 was a false positive.

**Method:**
1. Identify the 10 files named in `FIXES_TO_MAKE_VISUAL_AUDIT.md` V4.
2. Run the palette check (from P1) scoped to those files only.
3. If clean, update Evidence column with the finding. If not, add them to the P1 sweep.

#### P6 — A3 heading hierarchy audit

**Goal:** Fix semantic heading order on pages that currently skip levels.

**Method:**
1. For each page in `client/src/pages/`, grep `<h[1-4]` and note the sequence.
2. Flag any page where h3 appears without an h2 in the same section, or where h1→h3 skips occur.
3. Start with Bionomics: the section-header components render `<h3>` at `Bionomics.tsx:160,306,400` with no `<h2>` above them inside their sections. Either promote to `<h2>` or refactor the component to accept a `level` prop.
4. Proceed through the audit. Commit per page.
5. Update `FIXES_TO_MAKE_2026-04-18_POST_AUDIT.md` A3 row with Evidence.

#### P7 — OG image route-by-route wiring verification

**Goal:** Confirm each primary route's `<SEO />` call points at the correct OG image (1200×630).

**Method:**
1. Grep `<SEO` and `ogImage` across `client/src/pages/`.
2. For each route listed in `CLAUDE_CODE_PROMPT_2026-04-07_OG_IMAGES.md`, confirm the OG image prop is set and the file exists in `client/public/og/` at 1200×630.
3. Use `sharp` or `node:fs` + a PNG size parser if needed. Fix any wrong dimensions or missing props.
4. Close the doc row with Evidence.

#### P8 — Post-session deferred items

- V12 toast-garden and V13 quest-disclosure per `FIXES_TO_MAKE_2026-04-18_POST_AUDIT.md`.
- Fix 6 (2026-04-10) menu top bar overlap: blocked on Rye screenshot; leave in Queue.

---

### When to stop and hand back

Stop and summarize to Rye the moment:
- You hit an issue that requires Railway, Google Cloud, Loomio admin, Zapier toggle, or any other non-local service change.
- `pnpm typecheck` fails with an error that needs a design decision.
- You finish P1-P4 (at that point you've delivered enormous value and a second session is better than pushing past context limits).

### Final report format

End your session with a single markdown block titled `## Session Report 2026-04-XX` that contains:
1. A list of commits made this session (SHA + subject line).
2. The gate status at end of session (all 3 green, or which failed and why).
3. A list of FIXES rows moved from `CODED` to `VERIFIED` with Evidence.
4. Any new items that surfaced and were added to an existing FIXES doc or the audit.
5. A "Handoff to Rye" section listing any human-only steps that emerged.

### PROMPT END

---

### How Rye should use this prompt

1. Open Claude Code in the `regen-civics-clean` repo.
2. Copy everything between `### PROMPT START` and `### PROMPT END` above.
3. Paste as the first user message. Do not add anything else.
4. Let it run. Check in at the commit-batch boundaries.
5. When it reports back, run `pnpm typecheck` locally before the `git push`.

---

## Housekeeping notes

- A scratch file was created at `scripts/_check-mig-tmp.mjs` while trying to query the live DB. The sandbox's pnpm filesystem wouldn't permit deletion. Safe to `rm` on Rye's machine.
- Some symlinks were created under `node_modules/.pnpm/mysql2@.../node_modules/mysql2/node_modules/` in the same attempt. Also sandbox-locked for deletion. A `pnpm install` locally will restore a clean state.
- `.git/index.lock` is stale from 2026-04-17 13:50 and blocked `git mv` in this session. Rye should delete it manually before the next git operation.
