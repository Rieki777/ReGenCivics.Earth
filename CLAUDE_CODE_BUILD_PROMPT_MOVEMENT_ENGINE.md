# Superseded

This kickoff prompt described an earlier plan (Riverside-webhook trigger and a fetch, clean, chapters, upload video flow) that was not built that way. It is retired to avoid confusing future work.

Current sources of truth:

- `COORDINATION_ENGINE_WORKFLOW.md`: the canonical, current-state workflow (YouTube-poll pipeline, the two crons, the gate and reward, the flywheel).
- `MOVEMENT_COORDINATION_ENGINE_SPEC_2026-06-23.md`: the fuller vision and data model.
- `CLAUDE_CODE_PROMPT_2026-07-01_COMPLETE_COORDINATION_ENGINE.md`: the active build prompt for the remaining work.

This stub can be deleted or left to auto-archive.
